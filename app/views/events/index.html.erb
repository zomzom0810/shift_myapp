<%= render 'shared/header' %> 




  
<div id="events_calendar" class="calendar">
  
  <%= render partial: "calendar", locals: {events: @events} %>

</div>

<button type="button" class="btn btn-primary ml-4" data-toggle="modal" data-target="#new_event">
  ＋追加する
</button>


<div class="modal fade" id="new_event" tabindex="-1" role="dialog"aria-hidden="true">
   <%= render partial: "form", locals: {events: @events} %>
</div>




 <%= render 'shared/footer' %> 